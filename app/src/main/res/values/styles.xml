<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--<item name="android:windowDisablePreview">true</item>-->
        <!--<item name="android:windowNoTitle">true</item>-->
        <!--<item name="android:windowActionBar">false</item>-->
        <!--<item name="android:windowFullscreen">true</item>-->
        <!--<item name="android:windowContentOverlay">@null</item>-->
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- for text -->
    <style name="AppTabTextAppearance" parent="android:TextAppearance.DeviceDefault.Widget.TabWidget">
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/ltgray</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="AppTabLayout" parent="android:Widget.DeviceDefault.TabWidget">
        <!--<item name="tabMaxWidth">@dimen/tab_max_width</item>-->
        <item name="tabMinWidth">100dp</item>
        <item name="tabIndicatorColor">?attr/colorAccent</item>
        <item name="tabIndicatorHeight">4dp</item>
        <!--<item name="tabPaddingStart">6dp</item>-->
        <!--<item name="tabPaddingEnd">6dp</item>-->
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="tabTextAppearance">@style/AppTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/red</item>
    </style>

</resources>
